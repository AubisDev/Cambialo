import 'package:flutter/material.dart';

class UserAccountView extends StatelessWidget {
  static const name = "user-account";
  const UserAccountView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
